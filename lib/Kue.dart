import 'package:flutter/material.dart';

class Kue extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: ListView(
        children: <Widget>[Text('KsE')],
      ),
    );
  }
}
